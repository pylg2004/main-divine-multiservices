class AppStrings {
  AppStrings._();

  static const String appName = 'MAIN DIVINE MULTISERVICES';
  static const String defaultCurrency = 'HTG';
  static const String currencySymbol = 'G';

  /// Logo unique de l'entreprise, embarqué dans l'app (voir pubspec.yaml)
  /// — utilisé partout où un logo doit être affiché, pas de re-upload
  /// possible : c'est toujours le même.
  static const String logoAssetPath = 'assets/icon/logo.png';
}
